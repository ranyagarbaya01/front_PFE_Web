import { PharmaStockMedic } from './pharma-stock-medic.model';

describe('PharmaStockMedic', () => {
  it('should create an instance', () => {
    expect(new PharmaStockMedic()).toBeTruthy();
  });
});
